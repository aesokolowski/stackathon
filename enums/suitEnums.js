// since 0 is falsey in javascript, it seems like a good idea to start with 1
// when dealing with this language that technically doesn't have enums

const CLUBS = 1,
      DIAMONDS = 2,
      HEARTS = 3,
      SPADES = 4;

module.exports = {
  CLUBS,
  DIAMONDS,
  HEARTS,
  SPADES
};
